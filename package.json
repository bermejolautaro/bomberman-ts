{
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html --out-dir docs --public-url ./docs --no-minify"
  },
  "devDependencies": {
    "cssnano": "^6.0.0",
    "parcel-plugin-static-files-copy": "^2.6.0",
    "postcss": "^8.4.21",
    "typescript": "^4.5.5"
  },
  "staticFiles": {
    "staticPath": [
      {
        "staticPath": "sprites",
        "staticOutDir": "sprites"
      }
    ]
  }
}
